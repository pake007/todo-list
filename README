
== Requirements

gem install sinatra
gem install datamapper
gem install sqlite3-ruby
(ubuntu:$ apt-get install libsqlite3-dev libsqlite3-ruby)
gem install do_sqlite3

== setup

 * To make your sqlite database:

mkdir db
touch development.sqlite3
sqlite3 db/development.sqlite3
sqlite> CREATE TABLE todos (id INTEGER PRIMARY KEY, summary VARCHAR);

 * todolist.rb => edit configure, to do automigrate at first time

== run

ruby todolist.rb

== enjoy

http://localhost:4567
